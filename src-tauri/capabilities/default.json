{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Fenêtre principale — événements natifs (drag-drop fichiers), IPC existant.",
  "windows": ["*"],
  "permissions": ["core:default", "core:event:default", "opener:default"]
}
